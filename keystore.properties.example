# Copy this file to keystore.properties and fill in local/CI secrets.
# keystore.properties is ignored by git and must never be committed.

storeFile=release/quiettype-release.jks
storePassword=REPLACE_WITH_SECRET
keyAlias=quiettype
keyPassword=REPLACE_WITH_SECRET
